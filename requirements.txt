Flask==3.0.0
   pytube==15.0.0
   gunicorn==21.2.0
```
   - Click "Commit"

3. **Add `Procfile` file:**
   - Click "Add file" → "Create new file"  
   - Name: `Procfile`
   - Content:
```
   web: gunicorn main:app --bind 0.0.0.0:$PORT
